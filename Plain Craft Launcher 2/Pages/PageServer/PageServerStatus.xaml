<local:MyPageRight xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:PCL" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="PCL.PageServerStatus" PanScroll="{Binding ElementName=PanBack, Mode=OneWay}">
    <local:MyScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" x:Name="PanBack">
        <StackPanel x:Name="PanMain" Margin="25,25,25,10">
            <local:MyCard x:Name="CardArgument" Margin="0,0,0,15" Title="{DynamicResource Server.Status.Title}">
                <StackPanel Margin="25,40,25,15">
                    <StackPanel x:Name="WaitPanel">
                        <TextBlock Text="{DynamicResource Server.Status.WaitStatus}" HorizontalAlignment="Center" FontSize="14" Margin="0,10,0,10"/>
                    </StackPanel>
                    <StackPanel x:Name="DataPanel" Visibility="Collapsed">
                        <Image x:Name="ServerImage" Width="200" Height="100" Stretch="Uniform" HorizontalAlignment="Left" Margin="0,0,0,15"/>
                        <ContentControl x:Name="DescriptionContainer" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                            <TextBlock Text="{DynamicResource Server.Status.OnlinePlayers}"/>
                            <TextBlock x:Name="TxtOnlineCount" Text="--" Margin="5,0,0,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                            <TextBlock Text="{DynamicResource Server.Status.Version}"/>
                            <TextBlock x:Name="TxtVersion" Text="--" Margin="5,0,0,0"/>
                        </StackPanel>
                        <WrapPanel Margin="0,10,0,0" ItemHeight="50">
                            <local:MyButton MinWidth="120" Text="{DynamicResource Server.Status.Refresh}"
                                    Margin="0,0,0,10" Click="BtnRefresh_Click" />
                        </WrapPanel>
                    </StackPanel>
                </StackPanel>
            </local:MyCard>
        </StackPanel>
    </local:MyScrollViewer>
</local:MyPageRight>